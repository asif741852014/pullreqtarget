name: demo
on:
    pull_request_target: 
        branches: 
        - 'main'
jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: step1
          run: echo "${{ github.ref_name }}"
        